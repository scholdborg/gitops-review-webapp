{
  "name": "gitops-review-webapp",
  "version": "1.0.0",
  "description": "Tiny static webapp with an ArgoCD/GitOps-inspired flow: code change -> automated review -> build -> deploy to GitHub Pages only if checks pass.",
  "private": true,
  "type": "module",
  "scripts": {
    "review": "node scripts/simple-review.mjs",
    "review:file": "node scripts/review-file.mjs",
    "lint": "eslint .",
    "check:flow-language": "node scripts/check-flow-language.mjs",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp -r src dist/",
    "check": "npm run review && npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.5.0",
    "globals": "^17.7.0"
  }
}
